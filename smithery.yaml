name: filesystem-mcp-server
version: 1.0.0
description: MCP server for file system operations
author: Sujal Mane

# Use container runtime for Docker deployments
runtime: container

# Server will be accessible via HTTP/SSE
transport: sse

# HTTP transport configuration
startCommand:
  type: http
  url: http://localhost:8000/sse